export interface Scan {
  format: string;
  content: string;
  date: Date;
}

export interface Register {
  idAsignatura: string;
  seccion: string;
  asignatura: string;
  docente: string;
  correo: string;
  date: Date;
}
